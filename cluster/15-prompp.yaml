apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: prompp
  namespace: flux-system
spec:
  interval: 1m0s
  dependsOn:
    - name: kube-prometheus-stack
  wait: true
  path: ./components/prompp
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system