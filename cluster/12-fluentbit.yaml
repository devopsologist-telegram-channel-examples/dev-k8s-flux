apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: fluentbit
  namespace: flux-system
spec:
  interval: 1m0s
  dependsOn:
    - name: loki
  wait: true
  path: ./components/fluentbit
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system